import {StyleSheet} from 'react-native';

export const SplashStyles = StyleSheet.create({
  mainView: {
    padding: 0,
    height: '100%',
    width: '100%',
  },
  mainImage: {
    position: 'absolute',
    top: 0,
    right: 0,
    width: '100%',
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    height: '100%',
  },
});
